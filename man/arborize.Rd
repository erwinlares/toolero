% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arborize.R
\name{arborize}
\alias{arborize}
\title{Render a syntactic tree as a PNG image}
\usage{
arborize(
  tree,
  output = "syntactic-tree.png",
  dpi = 300,
  tree_notation = c("simple", "structured"),
  papersize = "a5",
  margin = "0.5cm",
  provenance = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{tree}{A character string. For \code{tree_notation = "simple"}, a
bracket notation string e.g. \code{"[S [NP] [VP]]"}. For
\code{tree_notation = "structured"}, a lingotree \code{tree()} call string.}

\item{output}{A character string. Path to the output PNG file.
Defaults to \code{"syntactic-tree.png"} in the current working directory.}

\item{dpi}{A numeric value. Resolution of the output PNG in dots per
inch. Defaults to \code{300}. Use \code{600} for print-quality output.}

\item{tree_notation}{A character string. One of \code{"simple"} (default)
or \code{"structured"}. Controls which Typst rendering backend is used.
See Details.}

\item{papersize}{A character string. Typst paper size for the
intermediate PDF. Defaults to \code{"a5"}. Increase to \code{"a4"} for very
wide trees.}

\item{margin}{A character string. Page margin for the intermediate
PDF. Defaults to \code{"0.5cm"}. Reduce for tighter crops around the
tree.}

\item{provenance}{A logical. Whether to write a companion \code{.yaml} file
recording the tree string and all rendering arguments alongside the
PNG. Defaults to \code{TRUE}. The provenance file has the same name as
the PNG but with a \code{.yaml} extension and lives in the same directory.
Pass \code{FALSE} to suppress it.}

\item{overwrite}{A logical. Whether to overwrite existing output files.
When \code{TRUE}, overwrites both the PNG and the provenance file if they
exist. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the path to the output PNG file.
}
\description{
Takes a syntactic tree and renders it using Quarto's Typst engine,
exporting the result as a PNG image. Supports two rendering backends
controlled by \code{tree_notation}:
}
\details{
\itemize{
\item \code{"simple"} uses \verb{@preview/syntree} and accepts a bracket notation
string, e.g. \code{"[S [NP [Det the] [N cat]] [VP [V sat]]]"}. This is
the most compact input format and suits basic linguistic trees.
\item \code{"structured"} uses \verb{@preview/lingotree} and accepts a nested
\code{tree()} call string. This backend supports per-node styling,
movement arrows, and multi-dominant trees.
}

The function is useful for producing standalone tree figures that can
be embedded in any document format -- LaTeX, Word, HTML, or
presentations -- without requiring a full LaTeX installation.

\code{arborize()} performs the following steps:
\enumerate{
\item Validates inputs and resolves the Typst package from \code{tree_notation}.
\item Builds a minimal \code{.qmd} document via \code{.build_arborize_qmd()}.
\item Writes the document and renders it inside a self-cleaning temporary
directory managed by \code{withr::with_tempdir()}.
\item Calls \code{quarto::quarto_render()} to produce an intermediate PDF via
Typst.
\item Converts the PDF to PNG using \code{pdftools::pdf_convert()}.
\item Reads the PNG bytes into memory before the temporary directory is
deleted, then writes them to the specified output path.
\item If \code{provenance = TRUE}, writes a companion \code{.yaml} file recording
the tree string and all rendering arguments.
}

On first use, Typst will download the required package from the Typst
package registry. This requires an internet connection. Subsequent
renders use the locally cached package.

Requires Quarto 1.4 or later with Typst support, and the \code{pdftools}
package for PDF-to-PNG conversion. Install \code{pdftools} with
\code{install.packages("pdftools")}.
}
\examples{
\dontrun{
# Simple bracket notation (default) -- also writes tree-1.yaml
arborize("[NP [Det the] [N cat]]", output = "my-trees/tree-1.png")

# Suppress provenance file
arborize("[NP [Det the] [N cat]]", provenance = FALSE)

# Wider tree with print-quality output
arborize(
  paste0(
    "[Aspectual Classes ",
    "[Statives [States]] ",
    "[Dynamic ",
    "[Atelic [Activities]] ",
    "[Telic ",
    "[Instantaneous [Achievements]] ",
    "[Durative [Accomplishments]]]]]"
  ),
  output    = "aspectual-classes.png",
  dpi       = 600,
  papersize = "a4"
)

# Structured notation using lingotree
arborize(
  "tree(
    tag: [VP],
    tree(
      tag: [DP],
      [every],
      [farmer]
    ),
    [smiled]
  )",
  tree_notation = "structured",
  output        = "vp-tree.png"
)
}
}
\references{
syntree Typst package (v0.2.1): \url{https://typst.app/universe/package/syntree}

lingotree Typst package (v1.0.0): \url{https://typst.app/universe/package/lingotree}
}
