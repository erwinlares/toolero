% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-clean-csv.R
\name{write_clean_csv}
\alias{write_clean_csv}
\title{Write a cleaned data frame to a CSV file}
\usage{
write_clean_csv(data, path, overwrite = FALSE, ...)
}
\arguments{
\item{data}{A data frame or tibble to write.}

\item{path}{A character string with the path to the output CSV file.}

\item{overwrite}{Logical. If \code{FALSE} (the default), errors if the file
already exists. Set to \code{TRUE} to overwrite an existing file.}

\item{...}{Additional arguments passed to \code{readr::write_csv()}, such as
\code{append}, \code{col_names}, or \code{quote}.}
}
\value{
Invisibly returns \code{path}.
}
\description{
\code{write_clean_csv()} writes a data frame to a CSV file using
\code{readr::write_csv()} and emits a cli confirmation message reporting
the number of rows and columns written. It is the natural counterpart
to \code{read_clean_csv()}, reinforcing the convention that \verb{data-raw/}
holds original inputs and \verb{data/} holds cleaned, analysis-ready outputs.
}
\details{
If column names are not already clean, \code{write_clean_csv()} applies
\code{janitor::clean_names()} before writing and emits a warning listing
the affected columns.
}
\examples{
\donttest{
sample_path <- system.file("templates", "sample.csv", package = "toolero")
data <- read_clean_csv(sample_path)

# Write to a temp file
out <- tempfile(fileext = ".csv")
write_clean_csv(data, out)

# Overwrite an existing file
write_clean_csv(data, out, overwrite = TRUE)

# Dirty names are cleaned automatically with a warning
dirty <- data.frame("First Name" = "Jane", "Last Name" = "Doe",
                    check.names = FALSE)
write_clean_csv(dirty, tempfile(fileext = ".csv"))
}
}
