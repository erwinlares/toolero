% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-citation.R
\name{generate_citation}
\alias{generate_citation}
\title{Generate a CITATION.cff file}
\usage{
generate_citation(
  filename = "CITATION.cff",
  path = ".",
  profile = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{filename}{Character. Name of the file to write. Defaults to
\code{"CITATION.cff"}, the name tools that read this format expect;
changing it means those tools will not find the file automatically.}

\item{path}{Character. Directory to write the file into. Defaults to
\code{"."} (the current working directory), matching the rest of the
family's project-scaffolding functions.}

\item{profile}{Character or \code{NULL}. Path to a profile file, typically
one written by \code{\link[=generate_profile]{generate_profile()}}. When supplied, every entry in the
profile's \verb{author:} block becomes an author entry in the generated
file. When \code{NULL} (the default), a single generic placeholder author
is written instead.}

\item{overwrite}{Logical. When \code{FALSE} (default), an existing file at
the destination is an error rather than being replaced.}
}
\value{
The path to the written file, invisibly.
}
\description{
Writes a Citation File Format (\code{CITATION.cff}) skeleton, optionally
pre-filled with author information from a profile written by
\code{\link[=generate_profile]{generate_profile()}}, so a project's citation metadata doesn't mean
retyping the same name, affiliation, and ORCID a third time.
}
\details{
\code{title}, \code{version}, \code{repository-code}, \code{url}, and \code{license} are project
facts a personal profile has no way to know, so they are left as
placeholders (some commented out) regardless of \code{profile}. \code{date-released}
is filled in with today's date, since that much is always knowable at
generation time; edit it later if the actual release date differs.

The given-names/family-names split needed by the Citation File Format is
done by splitting a profile's single \code{name} field on its last space,
which is right for the ordinary case and wrong for some real names.
Review the generated file's \code{given-names}/\code{family-names} fields before
relying on them, especially for a multi-word family name.
}
\examples{
\donttest{
generate_citation(path = tempdir())

profile_file <- tempfile(fileext = ".yml")
writeLines(
  'author:\n  - name: "Erwin Lares"\n    affiliation: "RCI, UW-Madison"\n    orcid: "0000-0002-3284-828X"',
  profile_file
)
generate_citation(path = tempdir(), profile = profile_file, overwrite = TRUE)
}

}
\seealso{
\code{\link[=generate_profile]{generate_profile()}}, the function that writes the file this
one can read from.
}
