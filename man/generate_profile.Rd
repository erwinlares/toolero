% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-profile.R
\name{generate_profile}
\alias{generate_profile}
\title{Generate a personal defaults file for create_qmd()}
\usage{
generate_profile(filename, path = fs::path_home(), overwrite = FALSE)
}
\arguments{
\item{filename}{Character. Name of the file to write, e.g.
\code{"profile.yml"} or a personal name like \code{"erwinlares.yml"}. Must be
supplied explicitly -- there is no default, so that keeping more than
one profile (a personal one and a work one, say) under different
filenames is a normal thing to do, not a workaround.}

\item{path}{Character. Directory to write the file into. Defaults to
the user's home directory (\code{fs::path_home()}), not \code{"."} -- unlike
\code{\link[=generate_project_config]{generate_project_config()}}, this file's whole purpose is being
reusable across every project rather than tied to one, so it belongs
somewhere that outlives any single project directory. Pass \code{path = "."}
if you'd rather keep a copy inside a specific project instead.}

\item{overwrite}{Logical. When \code{FALSE} (default), an existing file at
the destination is an error rather than being replaced.}
}
\value{
The path to the written file, invisibly.
}
\description{
Writes a YAML skeleton, pre-filled with placeholders and explanatory
comments, covering the author information and formatting preferences
that tend to be identical across every document you create with
\code{\link[=create_qmd]{create_qmd()}}. Edit the result with your own values, then pass its path
to \code{create_qmd(header_defaults = )} instead of retyping the same author
block and format options on every call.
}
\details{
The written file has two sections. Personal information becomes the
document's \verb{author:} block: name, affiliation, ORCID, email, and a
website URL, the fields Quarto's default HTML title block already knows
how to render. Document settings covers the recurring, non-personal
choices \code{create_qmd()} doesn't otherwise remember for you: \code{date}/
\code{date-modified}, \code{categories}, \code{lang}, \code{execute} options for quiet and
reproducible rendering, and a \verb{format: html:} block of layout
preferences. Neither section touches \code{css}, \code{include-before-body}, or
\code{include-after-body} -- those are \code{\link[=create_qmd]{create_qmd()}}'s \code{use_style} argument's
job, reading from a project's own \verb{assets/} folder, and a profile that
also tried to set them would collide with a specific project's branding
rather than complementing it.

The file is a plain copy of the packaged template, not a form filled in
programmatically -- this function does not prompt for values. Open the
written file, replace the placeholder values with your own, and delete
any key you don't want pre-filled; \code{create_qmd()} leaves a deleted key's
template placeholder untouched.

A phone number and mailing address are deliberately not among the
placeholders. Documents built from this file are the kind that tend to
get rendered to HTML and published, and neither belongs in something
public.
}
\examples{
\donttest{
generate_profile("my-profile.yml", path = tempdir())
}

}
\seealso{
\code{\link[=create_qmd]{create_qmd()}}, whose \code{header_defaults} argument reads the file
this writes; \code{\link[=generate_citation]{generate_citation()}}, which can also draw on it.
}
