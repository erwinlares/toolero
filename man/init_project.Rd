% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init-project.R
\name{init_project}
\alias{init_project}
\title{Initialize a new R project with a standard folder structure}
\usage{
init_project(
  path,
  use_renv = TRUE,
  use_git = TRUE,
  custom_folders = NULL,
  config = NULL,
  open = FALSE,
  branding = "none",
  uw_branding = deprecated(),
  use_readme = TRUE,
  use_rprofile = FALSE,
  scaffold_fn = renv::scaffold
)
}
\arguments{
\item{path}{A character string with the path and name of the new
project (e.g., \code{"~/Documents/my-project"}).}

\item{use_renv}{Logical. If \code{TRUE}, initializes \code{renv} in the new project.
Defaults to \code{TRUE}.}

\item{use_git}{Logical. If \code{TRUE}, initializes a git repository in the new
project. Defaults to \code{TRUE}.}

\item{custom_folders}{A character vector of folder names to add to or remove
from the project structure after the base set is resolved. Bare names
(e.g., \code{"models"}) add a folder. Names prefixed with \code{"-"} (e.g.,
\code{"-output/figures"}) suppress creation of that folder. When removing from
the built-in default set, only the named leaf is suppressed -- parent
directories are preserved, so \code{"-output/figures"} still leaves an
\verb{output/} folder behind. When removing from a set supplied via \code{config},
parents are not preserved: a config is an explicit and complete statement
of the structure, so nothing is added back that the author did not ask
for. Duplicates of existing folders generate a message and are skipped.
References to non-existent folders via \code{"-"} generate a warning.
\code{"-R"} is the one removal that does not take effect on disk: see
\code{config} below. Defaults to \code{NULL}.}

\item{config}{A character string. Path to a YAML project config file
produced by \code{\link[=generate_project_config]{generate_project_config()}}. When supplied, the folder list
in the config replaces the built-in standard structure entirely, and any
\verb{conventions:} it declares override the defaults key by key.
\code{custom_folders} is still applied on top of the config-derived set.
Defaults to \code{NULL}.

One folder cannot be suppressed, by a config or by \code{custom_folders}:
\verb{R/}. \code{\link[usethis:create_project]{usethis::create_project()}} creates it unconditionally, so it is
present in every project \code{init_project()} makes. A structure that
leaves it out is honored everywhere else -- \verb{R/} is absent from the
project manifest, gets no \code{.gitkeep}, and is not audited by
\code{\link[=check_project]{check_project()}} -- but the directory itself is there.}

\item{open}{Logical. If \code{TRUE}, opens the new project in RStudio after
creation. Defaults to \code{FALSE}.}

\item{branding}{Character or logical. Controls whether an \verb{assets/}
folder is created and populated. \code{TRUE} populates it with generic
placeholder branding files (\code{logo.png}, \code{favicon.png}, \code{header.html},
\code{footer.html}, \code{styles.css}). \code{"uw-madison"} populates it with
UW-Madison RCI branding files under the same standardized names.
\code{"none"} or \code{FALSE} creates no \verb{assets/} folder. Defaults to \code{"none"}.
When branding is enabled, \verb{assets/} joins the project's folder set and is
recorded in the project manifest alongside every other folder, so
downstream packages can find the branding files without being told about
them separately. Note that \code{favicon.png} is included in the asset set but
is not automatically wired into Quarto output -- favicons are a
website-project option set in \verb{_quarto.yml} rather than a per-document
HTML option.}

\item{uw_branding}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{branding}
instead. \code{uw_branding = TRUE} now maps to \code{branding = "uw-madison"};
\code{uw_branding = FALSE} maps to \code{branding = "none"}.}

\item{use_readme}{Logical or character. Controls whether a README file is
created at the project root. \code{TRUE} creates \code{README.md} from the
generalist toolero template. \code{FALSE} creates no README file. \code{"plain"}
creates \code{README.txt} with the same generalist content as \code{README.md} --
only the extension differs, not the content. Defaults to \code{TRUE}.}

\item{use_rprofile}{Logical or character. Controls whether the project's
\code{.Rprofile} also sources a personal one, so a project under \code{renv} does
not silently shadow the user's own startup customizations. \code{TRUE}
appends a guarded block that sources \verb{~/.Rprofile} if it exists. A
character string names a specific file to source instead -- useful for
a personal profile kept somewhere other than the platform default, such
as one tracked in a dotfiles repository (e.g.
\code{use_rprofile = "~/dotfiles/rprofile"}). \code{FALSE} adds nothing. If the
named file does not exist yet, a warning is issued when \code{init_project()}
runs, but the guard is still written -- see the "Personal \code{.Rprofile}
and renv" section below. Defaults to \code{FALSE}.}

\item{scaffold_fn}{A function. Called as \code{scaffold_fn(project = path)} when
\code{use_renv = TRUE}, in place of calling \code{\link[renv:scaffold]{renv::scaffold()}} directly.
Defaults to \code{renv::scaffold}. Exists for the same reason
\code{\link[=detect_execution_context]{detect_execution_context()}}'s \code{interactive_fn} does: so tests can
substitute a fake and exercise \code{use_renv = TRUE} without loading renv's
namespace into the test process. Overriding it outside of tests is
unsupported.}
}
\value{
Called for its side effects. Invisibly returns \code{path}.
}
\description{
\code{init_project()} creates a new R project at the given path with an
opinionated folder structure suited for research workflows. It optionally
initializes \code{renv} for package management and git for version control, and
records the structure it resolved in a project manifest at the project
root.
}
\section{The project manifest}{

\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{init_project()} writes \verb{_toolero.yml} to the project root, recording the
folder set it resolved and the naming conventions in force. The file
records the \emph{resolved} structure, never the inputs that produced it, so a
project built from a \code{config}, one built with \code{custom_folders}, and one
built from the defaults all produce the same shape of file and a reader
never has to replay anything to learn what the project looks like.

It exists because the structure is configurable. \code{\link[=check_project]{check_project()}} can
audit a customized project without being handed the same config again, and
\code{containr} and \code{submitr} can resolve where code, data, and outputs live
rather than assuming. Commit the file: it describes the project, not the
machine it was created on.

The format is experimental and may gain keys before it settles. The
\code{schema_version} field exists so that a reader can tell whether it
understands what it is holding.
}

\section{Empty folders and git}{

Each folder \code{init_project()} creates that is still empty when the call
finishes receives a zero-byte \code{.gitkeep}.

git tracks files rather than directories, so without this a scaffolded
structure survives nothing: the opening commit contains the files at the
project root and none of the layout, and a collaborator cloning the
repository gets a project with no folders in it. The placeholders are
written whether or not \code{use_git = TRUE}, since a project can be
git-initialized at any point afterwards.

Folders that already have content are left alone -- \verb{assets/} holds
branding files by then and is tracked on the strength of those.
}

\section{The active project}{

\code{init_project()} makes the new project the active \code{usethis} project for the
duration of the call, and restores whichever project was active before when
it returns. Nothing is left pointing somewhere the caller did not ask for.

This matters more than it sounds. \code{\link[usethis:create_project]{usethis::create_project()}} sets the
active project only for its own duration -- it uses
\code{\link[usethis:local_project]{usethis::local_project()}} internally and restores the caller's project on
exit when \code{open = FALSE}. Any step that resolves paths through the active
project therefore has to set it again explicitly. In v0.5.0 and earlier
\code{init_project()} did not, so with \code{use_git = TRUE} the \code{git} initialization
and its opening commit ran against whatever project happened to be active
in the calling session rather than the project just created.
}

\section{Dependency discovery and \code{renv}}{

When \code{use_renv = TRUE}, \code{init_project()} calls \code{\link[renv:scaffold]{renv::scaffold()}}, which
creates \code{renv/library}, \code{renv/activate.R}, \code{renv/.gitignore}, an
\code{.Rprofile} that activates the project in future sessions, and an initial
\code{renv.lock}.

The call is made through the injectable \code{scaffold_fn} argument rather
than by name, purely so tests can substitute a stand-in and never load
renv's namespace into the test process. Loading it there is what broke
\code{covr::package_coverage()}: renv's load hook takes over \code{.libPaths()},
and every package a test reaches with \code{::} afterwards (rather than one
already loaded) becomes unresolvable. Two environment variables papered
over the symptom before this argument existed; see
\code{covr-renv-incident.md} for the incident this closes out.

Three things changed here in v0.5.0, and they are worth understanding
together.

\code{renv::scaffold()} replaces \code{\link[renv:init]{renv::init()}}. \code{init()} loads the new project
into the \emph{calling} session, repointing \code{.libPaths()} at a library that is
empty apart from \code{renv} itself -- so every package the caller had
available vanishes until they restart R. Its \code{restart} argument suppresses
the restart, not the activation. That is reasonable behavior for someone
adopting \code{renv} in the project they are sitting in, and the wrong behavior
for a function whose job is to scaffold a project somewhere else.
\code{scaffold()} builds the same infrastructure and leaves the caller's
session untouched.

The \code{.renvignore} containing \verb{*.qmd} is gone. It excluded Quarto documents
from \code{renv}'s dependency discovery, which meant that a project whose
\code{library()} calls live in its \code{.qmd} source -- the arrangement this
package recommends -- could snapshot a lockfile with none of the analysis
packages in it, and \code{containr::generate_dockerfile()} would then build an
image that could not run the analysis. At the point the file was written
the project contained no \code{.qmd} files at all, so it never affected the
snapshot taken at creation time; its only effect was on every snapshot the
user took afterwards.

The snapshot at creation time is gone too, for the same underlying reason
the \code{.renvignore} was pointless there: a project that has just been
created has no code in it, so there is nothing to discover and nothing
worth recording. Take a snapshot yourself once the project has code, and
before containerizing:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{renv::snapshot()
}\if{html}{\out{</div>}}
}

\section{Personal \code{.Rprofile} and renv}{

R reads exactly one \code{.Rprofile} per session: the project's own if the
working directory has one, a personal one only if it does not. When
\code{use_renv = TRUE}, \code{\link[renv:scaffold]{renv::scaffold()}} writes a project \code{.Rprofile}
containing \code{source("renv/activate.R")}, and from that point on a user's
own startup customizations -- aliases, options, personal helper functions
-- are shadowed for every session opened in this project. Nothing warns
about this; it simply stops loading.

\code{use_rprofile = TRUE} appends a guarded block to the project's
\code{.Rprofile}, after renv's own activation line, that sources
\verb{~/.Rprofile} if it exists. Pass a character string instead of \code{TRUE} to
source a different file -- for instance a profile tracked in a dotfiles
repository rather than kept at the platform default location. Either
way, the existence check happens inside the written block, at every
session start, rather than being baked in once, so a file written or
edited after the project is created is still picked up. If the named
file does not exist yet when \code{init_project()} itself runs, a warning
says so -- most often a sign of a typo -- but the guard is written
regardless, since the file may simply not exist yet.

Defaults to \code{FALSE} because it cuts against renv's own isolation goal: a
project that automatically re-sources a personal environment is no
longer fully isolated from it. The \code{TRUE} mode is also written
generically -- the block sources whichever file sits at \verb{~/.Rprofile}
for whoever opens the project, not a specific person's file -- so a
collaborator who clones the project gets the same behavior the
project's original author chose, rather than one tied to a specific
person's home directory. A character path is written into the guard
exactly as given, so the same portability holds as long as the path
itself is written portably (\code{~}-relative, say, rather than an absolute
path tied to one machine).
}

\examples{
\dontrun{
init_project(path = file.path(tempdir(), "project1"),
             use_renv = FALSE, use_git = FALSE)

# Generic placeholder branding
init_project(path = file.path(tempdir(), "project2"),
             branding = TRUE, use_renv = FALSE, use_git = FALSE)

# UW-Madison RCI branding
init_project(path = file.path(tempdir(), "project2b"),
             branding = "uw-madison", use_renv = FALSE, use_git = FALSE)

# Add a folder and suppress one from the standard set
init_project(path = file.path(tempdir(), "project3"),
             custom_folders = c("models", "-output/figures"),
             use_renv = FALSE, use_git = FALSE)

# Drive structure entirely from a config file
init_project(path = file.path(tempdir(), "project4"),
             config = "~/linguistics-project.yml",
             use_renv = FALSE, use_git = FALSE)

# Plain-text README instead of Markdown (same content, README.txt)
init_project(path = file.path(tempdir(), "project5"),
             use_readme = "plain", use_renv = FALSE, use_git = FALSE)

# Skip the README entirely
init_project(path = file.path(tempdir(), "project6"),
             use_readme = FALSE, use_renv = FALSE, use_git = FALSE)

# Keep loading your own ~/.Rprofile customizations under renv (the
# scenario this argument exists for -- renv's own generated .Rprofile
# would otherwise shadow ~/.Rprofile entirely)
init_project(path = file.path(tempdir(), "project7"),
             use_rprofile = TRUE, use_git = FALSE)

# Source a personal profile kept somewhere other than ~/.Rprofile
init_project(path = file.path(tempdir(), "project8"),
             use_rprofile = "~/dotfiles/rprofile", use_git = FALSE)
}
}
\seealso{
\code{\link[=check_project]{check_project()}}, \code{\link[=generate_project_config]{generate_project_config()}}
}
