% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init-project.R
\name{init_project}
\alias{init_project}
\title{Initialize a new R project with a standard folder structure}
\usage{
init_project(
  file_path,
  use_renv = TRUE,
  use_git = TRUE,
  extra_folders = NULL,
  open = TRUE
)
}
\arguments{
\item{file_path}{A character string with the path and name of the new
project (e.g., \code{"~/Documents/my-project"}).}

\item{use_renv}{Logical. If \code{TRUE}, initializes \code{renv} in the new project.
Defaults to \code{TRUE}.}

\item{use_git}{Logical. If \code{TRUE}, initializes a git repository in the new
project. Defaults to \code{TRUE}.}

\item{extra_folders}{A character vector of additional folder names to create
inside the project. Defaults to \code{NULL}.}

\item{open}{Logical. If \code{TRUE}, opens the new project in RStudio after
creation. Defaults to \code{TRUE}.}
}
\value{
Called for its side effects. Does not return a value.
}
\description{
\code{init_project()} creates a new R project at the given path with an
opinionated folder structure suited for research workflows. It optionally
initializes \code{renv} for package management and git for version control.
}
\examples{
\dontrun{
# Create a project with the standard folder structure
init_project("~/Documents/my-project")

# Create a project with an additional folder
init_project("~/Documents/my-project", extra_folders = c("notebooks", "presentations"))

# Create a project without renv or git
init_project("~/Documents/my-project", use_renv = FALSE, use_git = FALSE)
}
}
