% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-data-doc.R
\name{generate_data_doc}
\alias{generate_data_doc}
\title{Generate a data documentation stub}
\usage{
generate_data_doc(dataset, path = "data", overwrite = FALSE)
}
\arguments{
\item{dataset}{A character string. The file name of the dataset this
doc describes, e.g. \code{"survey_responses.csv"}. Used to name the output
file (the extension is replaced with \code{.md}) and to fill in the doc's
title; must be supplied, since there is no reasonable default.}

\item{path}{A character string. Directory in which to write the file.
Defaults to \code{"data"}.}

\item{overwrite}{Logical. If \code{TRUE}, overwrites an existing doc at the
same location. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the full path to the written file.
}
\description{
Writes a Markdown file documenting a single dataset, with the standard
sections a data-management plan asks for -- source, date obtained,
license and usage terms, collection method, variables, and known issues
-- pre-filled where they can be, and left as placeholders where they
can't.
}
\details{
Recall that a research compendium's \verb{data/} and \verb{data-raw/} folders hold
the data itself, but not where it came from or what its columns mean.
\code{generate_data_doc()} fills that gap the same way \code{\link[=generate_citation]{generate_citation()}}
fills in for a missing \code{CITATION.cff}: a skeleton with a few fields
already filled in, ready for you to complete by hand.
}
\examples{
\dontrun{
generate_data_doc("survey_responses.csv")

generate_data_doc("weather_2024.csv", path = "data-raw")
}
}
\seealso{
\code{\link[=init_project]{init_project()}}, \code{\link[=generate_license]{generate_license()}}, \code{\link[=generate_citation]{generate_citation()}}
}
