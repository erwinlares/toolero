% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-license.R
\name{generate_license}
\alias{generate_license}
\title{Generate a LICENSE file}
\usage{
generate_license(
  license = "MIT",
  holder = NULL,
  year = format(Sys.Date(), "\%Y"),
  path = ".",
  overwrite = FALSE
)
}
\arguments{
\item{license}{A character string. One of \code{"MIT"}, \code{"CC0"}, or \code{"GPL-3"}.
Only this small, common set is supported; write a \code{LICENSE} file by
hand for anything else. Defaults to \code{"MIT"}.}

\item{holder}{A character string. The copyright holder -- a person or an
institution. Must be supplied explicitly; there is no default, since
guessing it wrong is worse than asking.}

\item{year}{A character string. The copyright year. Defaults to the
current year.}

\item{path}{A character string. Directory in which to write the file.
Defaults to \code{"."} (the current working directory).}

\item{overwrite}{Logical. If \code{TRUE}, overwrites an existing \code{LICENSE}
file at the same location. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the full path to the written file.
}
\description{
Writes a plain-text \code{LICENSE} file at the project root, using one of a
small set of common license templates with the copyright holder and year
filled in.
}
\details{
Note that this is different from how an R package normally carries a
license. A package's \code{DESCRIPTION} declares \verb{License: MIT + file LICENSE}
and pairs a two-line CRAN stub (\code{LICENSE}) with the full text in a
separate \code{LICENSE.md}. The projects \code{\link[=init_project]{init_project()}} scaffolds are
research compendia, not packages -- there is no \code{DESCRIPTION} to
reference a license from -- so \code{generate_license()} instead writes one
self-contained file carrying the license text directly, the way a plain
GitHub repository does.
}
\section{GPL-3}{

The GNU General Public License's full text runs to several hundred
lines. Rather than risk an inaccurate transcription of it,
\code{license = "GPL-3"} writes the license notice the Free Software
Foundation itself recommends attaching to a program, together with a
link to the canonical full text at
\url{https://www.gnu.org/licenses/gpl-3.0.txt}. \code{"MIT"} and \code{"CC0"} are both
short enough to reproduce here in full, and are written that way.
}

\examples{
\dontrun{
generate_license(license = "MIT", holder = "Jane Researcher")

generate_license(license = "CC0", holder = "Example Lab",
                  path = "~/Documents/my-project")
}
}
\seealso{
\code{\link[=init_project]{init_project()}}, \code{\link[=generate_data_doc]{generate_data_doc()}}
}
