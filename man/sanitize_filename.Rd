% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-by-group.R
\name{sanitize_filename}
\alias{sanitize_filename}
\title{Sanitize a string for use in a filename}
\usage{
sanitize_filename(x)
}
\arguments{
\item{x}{A character vector.}
}
\value{
A character vector of the same length.
}
\description{
Lowercases, replaces every run of non-alphanumeric characters with a
single \code{-}, and strips leading and trailing dashes.
}
\details{
The collapsing is what the rest of \code{\link[=write_by_group]{write_by_group()}}'s filename scheme
rests on: because a run of separators becomes exactly one dash, a
sanitized value can contain a single \code{-} but never two consecutive ones.
That leaves \verb{--} free to mark the boundary between grouping columns
without any possibility of colliding with content.
}
\keyword{internal}
