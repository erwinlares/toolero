% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-manifest.R
\name{.dedupe_accumulator}
\alias{.dedupe_accumulator}
\title{Deduplicate accumulator rows by file path}
\usage{
.dedupe_accumulator(accumulator)
}
\arguments{
\item{accumulator}{A data frame with the columns given by
\code{\link[=.accumulator_columns]{.accumulator_columns()}}.}
}
\value{
A data frame with the same columns, one row per distinct
\code{file_path}, ordered by \code{timestamp} ascending.
}
\description{
Internal helper used by \code{\link[=generate_manifest]{generate_manifest()}} to collapse an
append-only accumulator to one row per \code{file_path}, keeping the most
recent row for each.
}
\details{
The accumulator is append-only, so re-running an analysis within a
session leaves superseded rows behind. Rows are ordered by \code{timestamp}
-- which sorts correctly as a string, since \code{\link[=save_output]{save_output()}} writes UTC
with millisecond precision -- and the last row for each path is kept.
Position in the file breaks ties, so two rows sharing a timestamp
resolve in append order.

Note that the surviving row for a path may record a failure: if a save
succeeded and a later re-run of the same path failed, the failure is
what the manifest reports. This is the intended reading. The manifest
describes the state of the project at the end of the run, not the best
outcome observed along the way.
}
\keyword{internal}
