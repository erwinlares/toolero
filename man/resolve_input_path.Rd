% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve-input-path.R
\name{resolve_input_path}
\alias{resolve_input_path}
\title{Resolve the input data path for the current execution context}
\usage{
resolve_input_path(
  interactive = NULL,
  quarto = NULL,
  rscript = NULL,
  must_exist = TRUE,
  context = detect_execution_context()
)
}
\arguments{
\item{interactive}{The path to use in an interactive session. Typically
the local development copy of the data. If not supplied, falls back to
the document's own \code{params$input_file}, when the document declares one.}

\item{quarto}{The path to use while Quarto renders the document. Normally
\code{params$input_file}, which is also what is used when this argument is
not supplied.}

\item{rscript}{The path to use under \code{Rscript}, normally the first
command line argument. Defaults to exactly that.}

\item{must_exist}{Logical. Whether to check that the resolved path exists
on disk. Defaults to \code{TRUE}. Set it to \code{FALSE} when the path is a URL,
a database handle, or anything else \code{\link[fs:file_exists]{fs::file_exists()}} cannot see.}

\item{context}{Character. The execution context to resolve for, one of
\code{"interactive"}, \code{"quarto"}, or \code{"rscript"}. Defaults to
\code{\link[=detect_execution_context]{detect_execution_context()}}. Supply it directly in tests, or when the
caller has already computed it and does not want a second call.}
}
\value{
A single character string: the resolved path.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Picks an input file path according to where the code is running, then
checks that the result is usable and explains what went wrong when it is
not. It is the pattern this family of packages recommends everywhere,
packaged so that it lives in one place rather than being retyped into
every document.
}
\details{
Each of the three arguments is an ordinary R argument and therefore a
promise, so only the branch matching \code{context} is ever evaluated. Under
\code{Rscript}, where \code{params} does not exist, passing
\code{quarto = params$input_file} is safe because that expression is never
forced.

The reason this is a function rather than a documented \code{switch()} follows
from the same mechanism. When a branch \emph{is} selected and its expression
fails, the failure happens inside this function, where it can be caught
and explained. A document that declares no \verb{params:} block raises
\verb{object 'params' not found}, which says nothing about YAML headers; a
hand-written \code{switch()} in the document evaluates that expression in the
document's own frame, where nothing is in a position to intercept it.

The three ways a branch produces something unusable, and what this
function says about each:
\itemize{
\item Under \code{Rscript} with no argument passed, \code{commandArgs(trailingOnly = TRUE)[1]} is \code{NA_character_}. Reading that produces an error about
\code{NA} rather than about a missing argument.
\item Under Quarto, \code{params} exists only if the YAML header declares it, and
\code{params$input_file} is \code{NULL} if the block exists without that key.
\item In any context, the resolved path may simply not be there, which is
most often a working directory that is not what the author assumed.
}
}
\examples{
# Resolving for a named context. must_exist = FALSE because there is no
# such file here; in a real document you want the default.
resolve_input_path(
  rscript    = "data-raw/sample.csv",
  context    = "rscript",
  must_exist = FALSE
)

\dontrun{
# A path per context, which is what a scaffolded document shows. Only
# the branch matching the current context is evaluated, so the params
# reference is safe under Rscript, where params does not exist.
input_file <- resolve_input_path(
  interactive = "data-raw/sample.csv",
  quarto      = params$input_file,
  rscript     = commandArgs(trailingOnly = TRUE)[1]
)

# The defaults cover the mechanical branch and fall back to the
# document's own params for the other two, so a document whose YAML
# header declares input_file needs no arguments at all.
input_file <- resolve_input_path()
}
}
\seealso{
\code{\link[=detect_execution_context]{detect_execution_context()}}, which decides the branch.
}
