% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-project.R
\name{.renv_lock_is_bare}
\alias{.renv_lock_is_bare}
\title{Does a lockfile record no packages?}
\usage{
.renv_lock_is_bare(lockfile)
}
\arguments{
\item{lockfile}{Character. Path to a \code{renv.lock} file.}
}
\value{
A single logical.
}
\description{
Internal helper used by \code{\link[=check_project]{check_project()}}. Returns \code{TRUE} only when the
lockfile parses and records no packages other than \code{renv} itself.
}
\details{
\code{renv} is discounted because \code{\link[renv:scaffold]{renv::scaffold()}} installs it into the
project library and records it, so the lockfile of a freshly scaffolded
project is not literally empty. What matters is whether anything the
\emph{analysis} depends on is in there.

An unparseable lockfile returns \code{FALSE}: that is a different problem and
this helper should not report it as this one.
}
\keyword{internal}
