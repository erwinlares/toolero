% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save-output.R
\name{.capture_function_name}
\alias{.capture_function_name}
\title{Capture a readable name for a save function}
\usage{
.capture_function_name(f_expr, max_chars = 200L)
}
\arguments{
\item{f_expr}{A character vector, the result of
\code{deparse(substitute(.f))} evaluated in the caller's frame.}

\item{max_chars}{Integer. Maximum length of a returned anonymous-function
label before truncation.}
}
\value{
A single character string.
}
\description{
Internal helper used by \code{\link[=save_output]{save_output()}} to turn the unevaluated \code{.f}
argument into a readable label for the accumulator's \code{function_used}
column. Named and namespaced functions are recorded as written at the
call site. Anonymous functions fall back to a single-line, length-capped
label rather than a bare multi-line dump of the function body.
}
\details{
The returned string may contain literal braces when \code{.f} was anonymous.
It is written to the accumulator as data and is safe there, but any
\code{cli} message echoing this value must interpolate it (\verb{\{.val \{x\}\}})
rather than pasting it into the message template, or the braces will be
evaluated as inline markup.
}
\keyword{internal}
