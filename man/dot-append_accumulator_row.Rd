% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save-output.R
\name{.append_accumulator_row}
\alias{.append_accumulator_row}
\title{Append a row to the project accumulator}
\usage{
.append_accumulator_row(row, output_dir = "output")
}
\arguments{
\item{row}{A single-row data frame whose columns match
\code{\link[=.accumulator_columns]{.accumulator_columns()}} exactly, in order.}

\item{output_dir}{Character. Directory containing (or to contain)
\code{accumulator.csv}.}
}
\value{
The path to the accumulator, invisibly.
}
\description{
Internal helper used by \code{\link[=save_output]{save_output()}} to record one row of metadata to
\code{output_dir/accumulator.csv}, creating the file (with headers) on first
write and appending without headers thereafter.
}
\details{
When the accumulator already exists, its header is read and compared
against the expected schema before appending. A mismatch aborts rather
than appending misaligned rows to a file written under a different
schema version.

\code{NA} values are written as empty fields. Any reader of this file must
pass \code{na.strings = ""} to recover them as \code{NA} rather than as empty
strings.
}
\keyword{internal}
