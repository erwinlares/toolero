% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-manifest.R
\name{.read_accumulator}
\alias{.read_accumulator}
\title{Read the project accumulator}
\usage{
.read_accumulator(output_dir = "output")
}
\arguments{
\item{output_dir}{Character. Directory containing \code{accumulator.csv}.}
}
\value{
A data frame with the columns given by \code{\link[=.accumulator_columns]{.accumulator_columns()}},
all of type character.
}
\description{
Internal helper used by \code{\link[=generate_manifest]{generate_manifest()}} to read
\code{output_dir/accumulator.csv} back into a data frame, validating its
header against the expected schema.
}
\details{
A missing accumulator is an error rather than an empty result. The file
is created by the first \code{\link[=save_output]{save_output()}} call, so its absence means no
save was ever recorded -- most often because \code{save_output()} was called
with a different \code{output_dir}, or with \code{manifest = FALSE}, or was never
reached at all. Returning an empty manifest in that case would present a
setup mistake as a finished record.

Every column is read as character. Timestamps in particular must not be
coerced, since \code{\link[=.dedupe_accumulator]{.dedupe_accumulator()}} compares them lexicographically
and relies on the exact string written by \code{\link[=save_output]{save_output()}}.

Empty fields are read back as \code{NA} via \code{na.strings = ""}, matching the
\code{na = ""} convention used when the accumulator is written. Without this
the \code{error_message} column of every successful row would return as an
empty string rather than \code{NA}, and that difference would surface in the
manifest.
}
\keyword{internal}
