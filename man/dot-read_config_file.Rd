% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-project.R
\name{.read_config_file}
\alias{.read_config_file}
\title{Read and validate a project configuration file}
\usage{
.read_config_file(config, arg = "config")
}
\arguments{
\item{config}{Character. Path to the YAML file.}

\item{arg}{Character. Name of the calling argument, used in error
messages.}
}
\value{
A named list with elements \code{folders} (character vector) and
\code{conventions} (named list, defaults filled in for any key the file does
not supply).
}
\description{
Internal helper that parses a project configuration file and returns its
folder set and conventions. The same schema serves three roles -- a
hand-written config passed to \code{\link[=init_project]{init_project()}}, the \verb{_toolero.yml} a
project carries, and the audit target for \code{\link[=check_project]{check_project()}} -- so this is
the one place the schema is validated.
}
\details{
A file with no \code{schema_version} is treated as schema 1, so configs written
by earlier versions of toolero, which carried only a \verb{folders:} key,
continue to work unchanged.
}
\keyword{internal}
