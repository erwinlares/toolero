% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save-output.R
\name{save_output}
\alias{save_output}
\title{Save an object and record it in the project accumulator}
\usage{
save_output(
  object,
  file_path,
  .f,
  ...,
  manifest = TRUE,
  note = NULL,
  output_dir = NULL,
  config = NULL
)
}
\arguments{
\item{object}{The object to save.}

\item{file_path}{Character. A single destination path for \code{object}. Its
parent directory is created if it does not already exist, and the
creation is reported.}

\item{.f}{A function used to perform the save, called as
\code{.f(object, file_path, ...)}. Supply the function itself (for example
\code{saveRDS}, \code{ggplot2::ggsave}), not a call and not a string. Avoid
reassignment indirection (\verb{my_fn <- ggsave; .f = my_fn}) -- the
accumulator records the name exactly as written at the call site, so
this records \code{"my_fn"} rather than \code{"ggsave"}. Anonymous functions are
recorded as \code{"anonymous function: ..."} with the body collapsed to a
single truncated line.}

\item{...}{Additional arguments passed to \code{.f}.}

\item{manifest}{Logical. When \code{TRUE} (default), append a row to the
accumulator. When \code{FALSE}, \code{object} is still saved via \code{.f}, but
nothing is recorded.}

\item{note}{Character or \code{NULL}. An optional free-text note recorded
alongside this row.}

\item{output_dir}{Character or \code{NULL}. Directory containing (or to
contain) the accumulator. If \code{NULL} (the default) and \code{config} is
supplied, resolved from the config's \code{output_dir} convention; if
\code{config} is also \code{NULL}, falls back to \code{"output"}, the family-wide
convention, unchanged from earlier versions.}

\item{config}{Character or \code{NULL}. Path to a project configuration file
(typically a project's own \verb{_toolero.yml}, as written by
\code{\link[=init_project]{init_project()}}). Only consulted when \code{output_dir} is not supplied;
an explicit \code{output_dir} always wins. Defaults to \code{NULL}, which leaves
pre-0.5.1 behavior unchanged.}
}
\value{
\code{object}, invisibly. Called for its side effects.
}
\description{
\code{save_output()} writes \code{object} to \code{file_path} via
\code{.f(object, file_path, ...)}, then -- when \code{manifest = TRUE} (the
default) -- appends a row to the project-level accumulator at
\code{output_dir/accumulator.csv} recording what was saved, how, and whether
the write succeeded. The accumulator is the working file later consumed
by \code{\link[=generate_manifest]{generate_manifest()}}, which deduplicates it and reshapes it into
\code{project-manifest.json}.
}
\details{
The call to \code{.f} is wrapped in a narrowly-scoped \code{tryCatch()} -- only the
\code{.f(object, file_path, ...)} call itself, not the rest of
\code{save_output()}'s body. On failure, a row is still appended recording
\code{status = "failure"} and the caught message, after which the original
condition is rethrown unmodified. Its class, message, and call are
preserved as caught, so downstream handlers behave as though \code{.f()} had
been called directly. This is the one place in the package where the
\code{cli} convention is deliberately not followed: \code{cli::cli_abort()} would
construct a new condition and discard the original class.

\code{r_class} records \code{class(object)} as a single pipe-separated field,
captured before the write, since class cannot be reliably recovered from
the file afterward. Timestamps are recorded in UTC with millisecond
precision so that they sort lexicographically -- \code{\link[=generate_manifest]{generate_manifest()}}
relies on this when keeping the latest row per \code{file_path}.
}
\section{Project conventions}{

\code{config} is entirely opt-in. Nothing changes for a project never
scaffolded by \code{\link[=init_project]{init_project()}}: pass \code{output_dir} (or rely on the
\code{"output"} default) exactly as before. When \code{config} is supplied but
cannot be read, this aborts with the same message \code{\link[=init_project]{init_project()}} gives
for a bad \code{config}, rather than silently falling back to \code{"output"}.
}

\examples{
output_dir <- withr::local_tempdir()

save_output(
  object = mtcars,
  file_path = fs::path(output_dir, "mtcars.rds"),
  .f = saveRDS,
  note = "Unmodified example data.",
  output_dir = output_dir
)

}
\seealso{
\code{\link[=generate_manifest]{generate_manifest()}}
}
