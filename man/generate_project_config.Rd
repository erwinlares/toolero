% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init-project.R
\name{generate_project_config}
\alias{generate_project_config}
\title{Generate a project configuration file}
\usage{
generate_project_config(filename, path = ".", overwrite = FALSE)
}
\arguments{
\item{filename}{A character string. Name of the YAML file to create
(e.g., \code{"linguistics-project.yml"}). Must be supplied explicitly.}

\item{path}{A character string. Directory in which to write the file.
Defaults to \code{"."} (the current working directory). Consider using
\code{"~"} (your home directory) so the file is easy to reference in
future \code{init_project()} calls regardless of which project is active.}

\item{overwrite}{Logical. If \code{TRUE}, overwrites an existing file at the
same location. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the full path to the written file.
}
\description{
Writes a YAML configuration file pre-filled with the standard toolero
folder structure. Edit the file to define a custom project layout, then
pass its path to \code{\link[=init_project]{init_project()}} via the \code{config} argument.
}
\examples{
\dontrun{
# Write to the current working directory
generate_project_config("my-project.yml")

# Write to home directory for easy reuse across projects
generate_project_config("linguistics-project.yml", path = "~")

# Overwrite an existing config
generate_project_config("my-project.yml", overwrite = TRUE)
}
}
