% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-project.R
\name{.find_purled_qmds}
\alias{.find_purled_qmds}
\title{Find every .qmd stamped purl: true}
\usage{
.find_purled_qmds(path, folders)
}
\arguments{
\item{path}{Character. Path to a project directory.}

\item{folders}{Character vector. Folders declared for this project.}
}
\value{
A character vector of full paths to \code{.qmd} files whose header
declares \code{purl: true}, possibly empty.
}
\description{
Internal helper used by \code{\link[=check_project]{check_project()}} to locate the documents its
stale-purl check needs to look at. Searches the project root without
recursing, plus each declared folder with recursion, the same shape
\code{\link[=.project_has_sources]{.project_has_sources()}} uses and for the same reason: a full recursive
sweep would walk \code{renv/library}, which is slow and holds no documents of
the project's own.
}
\details{
A \code{.qmd}'s own header is read with \code{.split_yaml_header()} and parsed with
\code{yaml::yaml.load()} rather than matched with a regular expression,
because \code{.inject_purl_yaml()} always writes a real YAML boolean and this
should agree with however a reader would parse it, not with a pattern
that happens to work today.
}
\keyword{internal}
