% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init-project.R
\name{.add_rprofile}
\alias{.add_rprofile}
\title{Preserve access to a personal \code{.Rprofile}}
\usage{
.add_rprofile(path, source = "~/.Rprofile")
}
\arguments{
\item{path}{A character string. The project root.}

\item{source}{A character string. The file to source, written into the
guard exactly as given (e.g. \code{"~/.Rprofile"} or \code{"~/dotfiles/rprofile"}).
Resolved by \code{\link[=init_project]{init_project()}} from its \code{use_rprofile} argument.}
}
\value{
Called for its side effects. Returns \code{invisible(NULL)}.
}
\description{
Internal helper backing \code{\link[=init_project]{init_project()}}'s \code{use_rprofile}
argument. R reads exactly one \code{.Rprofile} per session: the one in the
current working directory if it exists, \verb{~/.Rprofile} only if it does
not. \code{renv::scaffold()} writes a project-level \code{.Rprofile} containing
\code{source("renv/activate.R")}, so once a project is under renv, whatever
aliases, options, or helper functions a user keeps in a personal
\code{.Rprofile} stop loading for that project -- silently, since nothing
errors.
}
\details{
This appends a guarded block to the project's \code{.Rprofile} (creating the
file if \code{use_renv = FALSE} left none behind) that sources \code{source}
if it exists, after renv's own activation line so the project library is
set up first. \code{source} is written into the block exactly as given --
never expanded or resolved to an absolute path here -- so a \code{~}-relative
value stays portable across whoever opens the project, the same way the
default \verb{~/.Rprofile} already does. The existence check happens at every
session start rather than once at project-creation time, so a file
written or edited after the project is created is still picked up.
}
\keyword{internal}
