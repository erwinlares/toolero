% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-citation.R
\name{.split_personal_name}
\alias{.split_personal_name}
\title{Split a full name into given and family names}
\usage{
.split_personal_name(name)
}
\arguments{
\item{name}{Character. A single full name, e.g. \code{"Erwin Lares"}.}
}
\value{
A named list with elements \code{given} and \code{family}, both character.
}
\description{
Internal helper used by \code{\link[=generate_citation]{generate_citation()}} to fill the
\code{given-names}/\code{family-names} fields the Citation File Format requires,
from the single \code{name} field a profile's \verb{author:} block carries (the
same field Quarto's own author schema uses).
}
\details{
The split is the last space in the string: everything after it is the
family name, everything before it is the given name(s). This is right
for the common case and wrong for some real names -- multi-word family
names ("van der Berg"), single-word names, and family-name-first
orderings all defeat it. \code{\link[=generate_citation]{generate_citation()}} flags this in its own
message rather than pretending the split is reliable.
}
\keyword{internal}
